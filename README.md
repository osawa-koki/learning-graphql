# learning-graphql

🦉🦉🦉 GraphQLについて学習するためのリポジトリです！  

## 実行方法

```shell
# クライアントサイド
cd ./client/
yarn install
yarn dev

# サーバーサイド
cd ./server/
yarn install
yarn db:reset && yarn db:create && yarn db:seed
yarn dev
```
